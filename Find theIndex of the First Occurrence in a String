class Solution {
public:
    int strStr(string haystack, string needle) {
        if (needle.empty()) return 0; // Some interpretations return 0 for empty needle
        size_t found = haystack.find(needle);
        if (found != string::npos) {
            return (int)found;
        } else {
            return -1;
        }
    }
};
